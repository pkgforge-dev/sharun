# sharun fork

This fork is inteded to be used by the [Anylinux-AppImages](https://github.com/pkgforge-dev/Anylinux-AppImages) project.

* It is going to be a simpler version `sharun` that only performs the operations we need.

* `lib4bin` is planned to be removed once all needed features have been added to [quick-sharun](https://github.com/pkgforge-dev/Anylinux-AppImages/blob/main/useful-tools/quick-sharun.sh).
